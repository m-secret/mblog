<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="club.msecret.mblog.mapper.UserMapper">
    <select id="findUserByTel" parameterType="long" resultType="club.msecret.mblog.entity.User">
        select * from user where tel=#{value}
    </select>
</mapper>